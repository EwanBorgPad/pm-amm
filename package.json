{
  "name": "pm-amm",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd anchor && anchor build --no-idl --ignore-keys && anchor idl build -o target/idl/pm_amm.json -t target/types/pm_amm.ts",
    "build:sdk": "pnpm --filter @pm-amm/sdk build",
    "sync:idl": "node scripts/sync-idl.mjs",
    "test": "cd anchor && anchor test",
    "test:rust": "cd anchor && cargo test --package pm_amm",
    "dev": "cd app && pnpm dev",
    "deploy": "bash -c 'source app/.env.local 2>/dev/null; solana program deploy anchor/target/deploy/pm_amm.so --keypair ~/.config/solana/id.json --program-id anchor/target/deploy/pm_amm-keypair.json --url \"${NEXT_PUBLIC_RPC_URL:-https://api.devnet.solana.com}\"'",
    "lint": "prettier --check 'app/src/**/*.{ts,tsx}' 'packages/*/src/**/*.ts' && cd app && pnpm lint",
    "lint:fix": "prettier --write 'app/src/**/*.{ts,tsx}' 'packages/*/src/**/*.ts' && cd app && pnpm lint:fix",
    "type-check": "pnpm --filter @pm-amm/sdk type-check && cd app && pnpm tsc --noEmit",
    "test:all": "cd anchor && cargo test --package pm_amm && cd ../oracle && python3 -m pytest test_oracle.py test_properties.py -v",
    "musdc": "cd anchor && ANCHOR_PROVIDER_URL=https://api.devnet.solana.com ANCHOR_WALLET=~/.config/solana/id.json pnpm exec ts-node --transpile-only -P ./tsconfig.json scripts/mint-usdc.ts",
    "seed": "bash -c 'source app/.env 2>/dev/null; cd anchor && ANCHOR_PROVIDER_URL=\"${NEXT_PUBLIC_RPC_URL:-https://api.devnet.solana.com}\" ANCHOR_WALLET=~/.config/solana/phantom.json pnpm exec ts-node --transpile-only -P ./tsconfig.json scripts/seed-markets.ts'"
  },
  "devDependencies": {
    "prettier": "^3.5.0"
  }
}
